# ========================================
# LLM Search Scout Configuration
# ========================================

# API Configuration
# Generate secure API keys with: openssl rand -hex 32
# Use comma-separated list for multiple keys
API_KEYS=demo-key-change-this-in-production

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=info

# SearXNG Configuration
# Internal URL for SearXNG service
SEARXNG_URL=http://searxng:8080
SEARXNG_SECRET_KEY=change-this-to-a-random-secret-key-now

# Search Configuration
MAX_RESULTS=50
DEFAULT_RESULTS=10
RATE_LIMIT_PER_MINUTE=60

# Content Extraction
MAX_CONTENT_LENGTH=5000
EXTRACT_TIMEOUT_SECONDS=10

# ========================================
# AI Features (Optional)
# ========================================
# Leave empty to disable AI features
# Get your key at: https://platform.openai.com/api-keys

OPENAI_API_KEY=

# ========================================
# Notes:
# ========================================
# - Restart containers after changing any values: docker compose restart
# - Keep API_KEYS secure and never commit them to version control
# - AI features (summarization, embeddings, deduplication) only work when OPENAI_API_KEY is set
# - Rate limiting is per API key, adjust based on your usage patterns
